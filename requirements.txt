fastapi==0.116.1
uvicorn[standard]
numpy
opencv-python-headless==4.12.0.88
python-multipart>=0.0.9

# Mac(M3 등) 로컬에선 silicon 빌드, 서버(Linux)엔 일반 mediapipe
mediapipe-silicon; platform_system == "Darwin" and platform_machine == "arm64"
mediapipe==0.10.14; not (platform_system == "Darwin" and platform_machine == "arm64")
